apiVersion: v1
kind: Service
metadata:
  name: {{ include "parimala.fullname" . }}
  labels:
    {{- include "parimala.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "parimala.selectorLabels" . | nindent 4 }}
